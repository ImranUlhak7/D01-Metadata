/**
  * This function is used to perform any required transformations on a work
  * order operation data object.
  *
  * @param {IClientAPI} context - The client context object
  * @param {Object} operationSSAM - The work order operation data object from SSAM.
  * @returns {Object} The work order operation data object with required transformations applied.
  */
export default function OperationDataBinding(context, operationSSAM) {
  return operationSSAM
}
