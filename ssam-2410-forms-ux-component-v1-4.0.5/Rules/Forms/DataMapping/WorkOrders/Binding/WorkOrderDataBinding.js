/**
  * This function is used to perform any required transformations on a work
  * order header data object.
  *
  * @param {IClientAPI} context - The client context object
  * @param {Object} workOrderHeaderSSAM - The work order header data object from SSAM.
  * @returns {Object} The work order header data object with required transformations applied.
  */
export default function WorkOrderDataBinding(context, workOrderHeaderSSAM) {
  return workOrderHeaderSSAM
}
